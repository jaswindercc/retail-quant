name: Overnight Scanner

on:
  # Run Mon-Fri at 4:15 PM New York (EDT) — after market close
  schedule:
    - cron: '15 20 * * 1-5'
  # Manual trigger
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scanner refresh
        run: python scripts/refresh_scanner.py

      - name: Cache overnight scanner data
        uses: actions/cache/save@v4
        with:
          path: dashboard/public/scanner_data.json
          key: overnight-scanner-data
